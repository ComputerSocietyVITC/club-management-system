import { Lexend } from "next/font/google";

const lexend = Lexend({ subsets: ["latin"] });

export default function Post() {
  return (
    <section className="flex items-center justify-center p-2 bg-transparent w-[35vw] h-[35vw] bg-black rounded-3xl shadow-[#486e83] shadow-2xl my-24">
      sam
    </section>
  );
}
